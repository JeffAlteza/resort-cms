import "./bootstrap";

import { Modal, Ripple, Carousel, Input, Datepicker,Dropdown, Collapse, initTE } from "tw-elements";

initTE({
    Carousel,
    Modal,
    Ripple,
    Input,
    Datepicker,
    Dropdown,
    Collapse,
});
